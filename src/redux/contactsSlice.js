// import { addContactAction } from "./actions,js";

// const initialState = {
//   items: [],
// };

// export const contactSlice = (state = initialState, action) => {
//   switch (action.type) {
//     case addContactAction.type:
//       return {
//         ...state,
//         items: [...state.items, action.payload],
//       };
//     default:
//       return state;
//   }
// };
